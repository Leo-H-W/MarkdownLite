待实现的功能清单

|序号|内容|状态|备注|
|---|---|---|---|
|1|编辑模式下，每隔3s自动保存文件|Done||
|2|可以新增文件，保存在当前目录|Done|「新文件」按钮（在「新tab页」之前），带文件名校验与异常提示|
|3|实现传统模式和现代模式（不需要切换编辑/浏览的模式）typora模式，要求如下<br>a. 新增切换按钮，传统模式/现代模式<br/>b. 传统模式，保持现在的效果，即有 编辑 和 浏览 按钮<br/>c. 现代模式，没有 编辑 和 浏览 按钮，效果保持和typora这个软件一致，你先弄清楚它的效果|Done||
|4|把markdown文件拖入界面时新开tab页打开，并尽量定位到所在目录（仅限文件在当前已加载目录内或拖入文件夹的情况，浏览器安全限制无法从单个文件获取父目录）|Done||
|5|修改bug，现在文字里面有 ~ 会把文字加上删除线，本意是范围 例如1~100|Done||
|6|左侧界面加上文件浏览窗口，显示当前目录下所有支持打开的文件，可以切换文件列表窗口和目录|Done|递归子目录，按目录分组，带过滤框|
|7|现代模式下渲染图片和 Mermaid 图表|Todo|现均为源码形态：图片不做 resolveImages（`app.js:1693-1695`）、Mermaid 直接 return（`app.js:1725-1726`）、样式注释写「第一版保留源码形态」（`style.css:1170`）。现代模式只有 CodeMirror 一个渲染面，需把源码 span 替换成真实节点（`replacedWith`）或另挂渲染层|
|8|现代模式，在表格的表头后面增加 ... 菜单，点开后支持 删除表格，点击删除表格后，整个表格内容删除|Done|表头行（光标/鼠标落在该行时）整表右边界外出现「…」，菜单里只有「删除表格」；删表头 + 分隔行 + 全部数据行，前后空行不动，一次原子编辑可 `Ctrl+Z` 一步恢复。顺带修了表格块边界判定：两张表紧邻（中间没空行）时第二张表的表头被吞进第一张表（`1f803ec` 的注释说了这个问题但没真正修掉），现抽出 `tableDataEnd()` 三处共用。回归见 `scripts/table-menu-check.mjs`（31 项）|
|9|默认使用现代模式（新用户 / 清过站点数据时直接进常驻编辑态）|Done|`loadModePreference` 的判据必须写成「值等于 `'traditional'` 才回传统」—— 写成「等于 `'modern'` 才现代」的话，键缺失时会落回传统，这正是改名换键后老用户「一打开变成传统模式、点正文既没光标也不能编辑」的成因。配套：启动时把旧键 `lightmdkit.*` 一次性搬到 `markdownlite.*`；模式开关由按钮改成滑动开关、标签显示**当前**模式（旧按钮写的是「点它切到哪」，传统模式下显示「现代模式」，被误读成状态说明，就是这次误报的起点）|

