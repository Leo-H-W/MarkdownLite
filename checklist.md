# 验收清单：现代模式表格 / 自动保存

这里逐条记录**提出的期望效果**，作为每次改动后的验收依据。改动完必须跑一遍：

```bash
node server.js                      # 另开一个终端，保证 http://localhost:3456 在跑
node scripts/checklist-check.mjs    # 表格相关的 R/D/K/M/C 各项
node scripts/autosave-check.mjs     # 自动保存的 A 各项
```

脚本按下面的编号逐项校验并打印结果，**全项通过才算改完**；「来源」一列是原始诉求，
「怎么验」一列写明判定方式。新增期望就往表里加一条，同时在脚本里补一个同名用例。

## 期望效果

| 编号 | 期望效果 | 来源（原话） | 怎么验 |
| --- | --- | --- | --- |
| R1 | 单元格里的 `<br>` 渲染成真换行：那一格按内容分成多行、格子变高 | 「换行并没有正常显示」 | 两行 `<br>` 的格子行高 = 74px（单行 37px），内容竖排 |
| R2 | 连续多个 `<br>` 各占一行，空行也是一整行 | 同上（`1<br><br><br><br><br>dd`） | 四个 `<br>` 的行高 = 149px，各段 y 依次相差 37px |
| R3 | 换行格所在行的其它格子不被挤走：列位与普通行对齐，单行格撑满整行高 | 「直接把后面的单元格挤到右边去了」 | 多行行各列 x = 普通行各列 x；同行单行格高 = 整行高 |
| R4 | 点击多行格里的某一段，光标落到那一段对应的字符；光标画在所在段内而不是整行高 | 同上（点击要能改那一行） | 点第三段落 4:13；`.CodeMirror-cursor` 落在该段矩形内、高度 < 37px |
| R5 | `Ctrl+Enter` 插入 `<br>` 后该行多一行；删掉最后一个 `<br>` 退回单行渲染 | 「单元格内换行」（既有能力） | 插后行高 112px、行类名含 `cm-tbl-mline3`；删后 37px 且无 `cm-tbl-mline` |
| D1 | 删 `<br>` 时整段删：光标在它里面或紧贴待删一侧，一次按掉 4 个字符 | 「整个`<br>`都删掉，现在是删了一个字符」 | `<br>` / `<br/>` / `<br />` 三种写法、两个方向、光标在内/贴前/贴后各一次 |
| D2 | 表格里的内容操作不影响表格骨架：管道符、隔壁单元格的内容、行边界都不许被删掉 | 「表格里面的内容操作不要影响表格骨架本身了」「把后面单元格去掉了」 | 每行每个光标位置各按一次 Delete/Backspace，管道符数量不变；格末连按 8 次 Delete 内容一字未动 |
| D3 | 单独一行 `||`（不是真表格）能被正常删掉 | 「`||` 删不掉（用 backspace 不行）」 | Backspace 两次：`||` → `|` → 空 |
| D4 | 表格外的 `<br>` 是普通文字，仍逐字删 | 同上（保护只针对真表格） | 段落里的 `<br>` 按一次 Backspace 只少一个字符 |
| K1 | 在本行最后一个单元格按 Tab：在**本行之后**新增一行 | 「在该行之后新增一行」「新增的行新增到最后去了」 | 第 4 行尾按 Tab → 新行落在第 5 行，原第 5 行下移；首行/末行同理 |
| K2 | 不是最后一格的单元格按 Tab：不新增行 | 「不是这行的最后一个单元格里面按 tab 也新增了一行」 | 按后文档行数不变，光标右移到下一格 |
| K3 | 表头行 / 分隔行按 Tab：插在分隔行之后（表格结构不散） | 由 K1 推出（表头没有「行尾」） | 表头尾按 Tab → 序列为 表头 / 分隔行 / 新行 / 原首行 |
| K4 | Enter 跳到下一行同一格；在最后一行则在其后插入一行（既有能力不被带坏） | 既有能力 | 中间行 → 下一行同一格；末行 → 末尾插一行并跳过去 |
| K5 | Tab 在表格外走默认缩进行为（不插行） | 既有能力 | 正文按 Tab 不改变行数 |
| C1 | 点单元格内容的末尾（含点该格文字右侧的空白区）→ 光标落在该内容之后 | 「点击…单元格 制造业 的 业 后面，鼠标光标却在 造 后面」 | 量出该格文字的矩形：点右边缘 -2px / 点文字与格右边界之间的空白 → 光标都落在内容末尾（`2:4`） |
| C2 | 点某个字的右半边 → 光标落在该字之后；点左半边 → 落在之前 | 同上（精确到字） | 「制造业」按字量：点「制 / 造 / 业」各自右半边 → 2:2 / 2:3 / 2:4 |
| C3 | 点单元格左侧内边距 → 光标落在该格内容之前 | 同上 | 点 `box.left + 3` → 2:1 |
| C4 | 含 `<br>` 的多行行里，点某一段的文字末尾 → 光标落在该段之后 | 同上（多行格同样要准） | 点第二段「bb」行尾 → 5:9 |
| C5 | 光标落在单元格内容末尾（= 贴在管道符前）时，**竖线画在文字的末尾**、不是格子的右边界；点击与键盘移动都一样 | 「点 业 后面，鼠标光标却在 造 后面」的延续：位置对了，画的地方也要对 | 量点击后 `.CodeMirror-cursor` 的 x 与「业」字形的右边缘，相差 ≤1px；键盘把光标移到该位置同样 |
| M1 | 光标（或鼠标）落到某个数据行时，在这行**最后一格之外**（表格右侧）出现「…」；其它行不显示 | 「当鼠标焦点定位到某行时，在这行的最后一个单元格的外面增加…」 | 光标移到某行后：只有该行按钮 `opacity = 1`；按钮 x ≥ 最后一格右边界 |
| M2 | 点「…」弹出菜单，菜单里有「删除本行」；点它删掉该行，表格结构不受损 | 「点击这… 弹出菜单，实现可以删除这行的效果」 | 点按钮 → 菜单出现且含「删除本行」；点它 → 该行消失、列数不变、其余行内容不动 |
| M3 | 表头行与分隔行**没有**「…」（删了表格就不再是表格） | 由「不要改动引发」推出 | 按钮数量 = 数据行数；表头 / 分隔行内无按钮 |
| M4 | 含 `<br>` 的多行行同样有按钮，位置也在最后一格右侧 | 同上（现代模式表格的既有能力） | 多行行的按钮 x ≥ 该行最后一格右边界 |
| M5 | 点菜单外、按 Esc、编辑内容都收起菜单；点按钮本身不会把光标挪走 | 由交互常识推出 | Esc / 外部点击后菜单消失；点按钮前后光标位置不变 |
| S1 | 顶栏「自动保存(5s)」开关**默认关闭** | 「页面顶部工具栏加个自动保存的开关，默认不打开」 | 全新上下文打开页面：按钮不是开启态；`localStorage['markdownlite.autosave']` 不为 `on` |
| S2 | 开关关着时不做任何自动写盘（编辑、停手都不写） | 同上（默认关 = 默认安全） | 关闭状态下输入一个字符再等 8 秒：写入次数 = 0 |
| S3 | 点击开关打开；打开时把已有未保存的改动也排上 | 「可以点击打开」 | 点击后按钮带 active（开启态）；约 5 秒后写一次，内容含开启前的改动 |
| S4 | 再点一下关闭，立刻停止自动写盘 | 同上 | 关闭后输入并等 8 秒：写入次数 = 0 |
| S6 | 点开关**不产生状态栏提示**，工具栏按钮不位移 | 「按钮后面的提示 去掉，不然按钮的位置会乱动」 | 点击开关前后，工具栏右侧各按钮的 x 坐标完全一致 |
| S5 | 开关状态记在浏览器本地，重开页面仍生效 | 由应用既有偏好机制推出 | 打开后 reload：按钮仍为开启态，自动保存照常工作 |
| A1 | （开关打开后）内容没变就不保存（不许定时无条件整篇重写文件） | 「不要无条件乱保存」 | 光标不动、不输入，静置 8 秒：写入次数 = 0 |
| A2 | （开关打开后）改动后**静止 5 秒**才写一次，5 秒内还在改就重新计时 | 「只有页面没有变动之后…才自动保存」「把 3 秒改为 5 秒」 | 输入一个字符：3 秒内不写；约 5 秒时写恰好 1 次；连打 3 个字符仍只写 1 次（在最后一次之后） |
| A3 | 写进文件的必须是改动后的内容 | 同上 | 上次写入的文本包含刚输入的字符 |
| A4 | 改了又改回去（内容与上次写盘一致）不写 | 同上（别做无谓重写） | 输入一个字符再精确还原：写入次数不变 |
| A5 | 带着没保存的改动切走（切模式）时有兜底保存，不丢内容 | 由「自动保存」既有能力推出 | 输入后切回传统模式：恰好多写 1 次，内容含最后一次改动 |
| E1 | 把单元格内容删空后，格子的盒子不能消失、行高不能被压扁（骨架仍完好） | 「在空白的单元格里面按 delete 按键，会把表格结构破坏，这个单元格的行高度会变很窄」 | 空表格行删空第 1 格 / 整行删空 / 有内容的行里删空中间格：三种情况下该行仍是 3 个格子盒子、行高仍 37px、管道符一个不少 |
| B1 | 普通（无 `<br>`）表格的渲染与交互保持原样：列宽、列位、点击落点、分隔行展开 | 「不要改动引发」 | 普通行高度 37px、列位与改动前一致；表格外行为不变 |

## 已知的既有边界（不算缺陷，改动时别误伤）

- 单元格内拖拽选字会被点击落点修正拦截（改用键盘选区）；表格单元格里的选区高亮被白底盖住，看不见。
- 两张表紧邻（中间没有空行）时，在上一张表最后一格按 Tab，新行会插到下一张表表头之后 —— 这是表边界判定的既有行为（`findTableAtCursor` 会把紧邻的下一行算进本表）。
- 顶到行首 / 行尾的 Delete / Backspace 不接管换行：想删整行或删管道符，选中后删。
